{{#each imports}}import {{name}} from {{json from}};{{getNewLine}} {{/each}}

export default class {{varName}} extends {{baseRouter}}  {

    constructor(basePath: string){
        super(basePath);
    }
}