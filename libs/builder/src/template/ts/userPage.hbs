import {{capitalizeFirstLetter pageName}}Design from '{{designFilePath}}';
import { withDismissButton } from '@smartface/mixins'

 export default class {{capitalizeFirstLetter pageName}} extends withDismissButton({{capitalizeFirstLetter pageName}}Design) {
	constructor(){
		super();
  }

	/**
	* @event onShow
	* This event is called when a page appears on the screen (everytime).
	* @param {Object} parameters passed from Router.go function
	*/
	public onShow(parameters: any ) {
		super.onShow?.()
	}

	/**
	* @event onLoad
	* This event is called once when page is created.
	*/
	public onLoad() {
		super.onLoad?.()
	}
}



